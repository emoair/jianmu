# JianMu v0.3 路线图

当前 MVP 的边界：在 C 语言整数求和这个极小程序域内，通过字符串拼接 emitter 生成 main.c，经 gcc 编译验证。以下方向说明当前局限和下一阶段扩展。

---

## 已完成（v0.3）

- **支持 values 参数传递**：从"1加2加3"提取 `[1, 2, 3]`，`VariableDefinitionExpert` 按实际值初始化变量，编译执行输出 6，correctness signal 具有真实区分度。
- **支持英文 "sum of three numbers"**：`IntentRouter` 新增 three/numbers 关键词匹配，英文基础表达可命中。
- **支持 "多加一个X" 扩展值**：`ExpandSumExpert` 接受 `new_value` 参数，追加指定值的变量。
- **Scoring 分离 correctness/runtime**：`score()` 返回独立的 `correctness_score` 和 `runtime_score`。
- **TraceCache 写入条件改为 correctness_score==1.0**：只缓存正确结果，避免错误路径污染缓存。

---

## 1. tree-sitter 或 clang AST 接入（替代字符串 emitter）

**目标**
用真实 AST 表示程序结构，替代当前直接拼接字符串生成 C 代码的方式。

**当前差距**
当前 emitter 是字符串模板拼接，没有语法树。任何结构变化（如插入语句、修改表达式）都是字符串操作，容易产生语法错误且无法做结构验证。ProgramIR 目前是 Python 数据结构，不对应任何真实 AST 节点。

**最小可验证实验**
用 tree-sitter-c 解析当前生成的 main.c，断言 AST 节点数量和结构与预期一致（变量声明数 = N，加法节点数 = N-1）。通过则说明生成代码结构可被 AST 验证，为后续 rewrite 打基础。

---

## 2. 从字符串 emitter 升级到真实 AST rewrite

**目标**
Expert 操作直接修改 AST 节点，而非拼接字符串，生成代码由 AST unparse 产生。

**当前差距**
当前每个 Expert 返回字符串片段，由 emitter 拼接。这意味着 Expert 之间没有结构约束，可以生成语法上合法但语义错误的代码（如重复变量名、表达式引用未声明变量）。

**最小可验证实验**
实现一个最小 AST 节点集（VarDecl、AssignStmt、BinaryExpr、ReturnStmt），Expert 操作这些节点，最终由 unparser 生成 C 代码。对两数/三数求和场景，验证生成代码与当前字符串版本 byte-level 一致。

---

## 3. ✅ 支持变量值变化（例如 1+2+3）[已完成，v0.3]

已实现 values 提取和传递链路：`IntentRouter` 从输入中提取 `values` 列表，写入 `ProgramIR` 的 `values` 字段，`VariableDefinitionExpert` 用实际值初始化变量，编译执行后输出正确结果（如 1+2+3=6）。

**下一步扩展**（见"下一步路线"节）：支持负数、零、多位数、中文顿号/逗号分隔。

---

## 4. 支持乘法、减法、括号优先级

**目标**
支持混合运算表达式，如 `(a+b)*c`，Expert 能正确处理运算符优先级。

**当前差距**
当前 SumExpressionExpert 只处理加法，BinaryExpr 节点没有运算符字段，也没有括号节点。扩展到乘法/减法需要重新设计 ProgramIR 的表达式表示。

**最小可验证实验**
在 ProgramIR 中增加 `operator` 字段和 `parenthesized` 标志。实现 `(a+b)*c` 的生成，编译执行后断言结果正确。用至少 3 个不同表达式结构验证优先级处理无误。

---

## 5. 支持单元测试生成

**目标**
系统生成的 C 程序附带单元测试（assert 或独立 test main），可自动验证多组输入输出。

**当前差距**
当前只生成一个 main.c，用单组固定输入验证。没有测试框架集成，没有多组输入覆盖，correctness signal 只来自"编译通过 + 单次执行不崩溃"。

**最小可验证实验**
生成包含 3 组 assert 的 test_main.c（如 sum(1,2,3)=6, sum(0,0,0)=0, sum(-1,2,3)=4），编译执行，断言全部 assert 通过。这是比当前更强的 correctness signal。

---

## 6. 支持 execution feedback Q-table

**目标**
记录每个 Expert 序列在不同 intent 下的执行结果（成功/失败/编译错误），用 Q-table 指导后续 Expert 选择。

**当前差距**
当前 Expert 选择是确定性规则路由，没有任何学习或反馈机制。Trace Cache 只缓存成功路径，不记录失败原因，无法从失败中更新策略。

**最小可验证实验**
实现一个最小 Q-table（key: intent_type × expert_sequence_prefix，value: success_rate）。在 10 次不同 intent 请求后，验证 Q-table 中至少有 3 个不同 key 被更新，且成功路径的 Q 值高于失败路径。

---

## 7. 支持 speculative execution：两条候选路径比较

**目标**
对同一 intent，并行生成两条不同 Expert 序列的候选程序，编译执行后选择正确的一条。

**当前差距**
当前是单路径确定性生成，没有候选比较机制。如果唯一路径失败，系统无法自动尝试备选方案。

**最小可验证实验**
对"三数求和"intent，生成两条路径：路径 A（当前标准路径）和路径 B（故意引入一个小变体，如不同变量命名）。两条路径都编译执行，验证系统能识别哪条输出正确，并将正确路径写入 Trace Cache。

---

## 8. 支持小型 L1 semantic classifier

**目标**
用一个轻量分类器（非 LLM）将自然语言输入归一化到结构化 intent，替代当前基于关键词的字符串匹配。

**当前差距**
当前 Intent Router 是关键词规则匹配（如检测"sum"、"加"等词），对变体输入（如"把三个数加起来"）鲁棒性差，没有任何统计或向量化表示。

**最小可验证实验**
收集 20 条自然语言变体（覆盖两数/三数/四数求和），用 TF-IDF + 逻辑回归训练一个最小分类器，在留出集上准确率 ≥ 90%。与当前关键词匹配对比，记录哪些变体当前方法失败而分类器成功。

---

## 9. 支持真实 C 文件 patch，而不是只生成 main.c

**目标**
系统能修改已有 C 文件（如在现有函数中插入语句、修改表达式），而不只是从零生成 main.c。

**当前差距**
当前每次都生成完整的 main.c，无法处理"在已有代码基础上修改"的场景。这是与真实软件工程任务的最大差距之一。

**最小可验证实验**
给定一个已有 C 文件（包含 `int sum(int a, int b) { return a+b; }`），系统将其 patch 为三数求和版本。验证 patch 后文件编译通过，原有函数签名保留，只有函数体被修改。

---

## 10. 建立 benchmark 表格

**目标**
建立一个可重复运行的 benchmark，量化系统在不同 intent 变体上的成功率、编译率、执行正确率。

**当前差距**
当前没有系统性 benchmark，只有少量手写测试用例。无法回答"系统在多少比例的输入上正确"这个基本问题，也无法与其他方法比较。

**最小可验证实验**
定义 benchmark 集：20 条 intent（覆盖两数/三数/四数求和，含自然语言变体），记录每条的编译是否通过、执行输出是否正确、是否命中 Trace Cache。输出一张表格，包含这三列指标。这是论文中"实验结果"部分的最低要求。

---

## 下一步路线（替代原第3条）

以下为数值泛化的最小扩展方向，优先级从高到低：

1. **支持负数**（如 -1+2+3=4）：`IntentRouter` 解析负号，`VariableDefinitionExpert` 支持负整数初始化。
2. **支持 0**（如 0+0+0=0）：边界值测试，验证 correctness signal 对零值的区分度。
3. **支持多位数**（如 10+20+30=60）：解析多位整数，验证 emitter 生成正确。
4. **支持中文顿号/逗号**：解析"1、2、3"或"1,2,3"格式的数值列举。
5. **支持乘法、减法、括号优先级**：需重新设计 ProgramIR 表达式表示（见第4条）。

---

## 优先级说明

**最高优先级**：先做负数/零/多位数（最小扩展，不改变架构，直接加固数值泛化覆盖）。

**次优先级**：benchmark 表格（直接支撑论文"实验结果"部分，reviewer 必问）。

**后续**：中文顿号/逗号、乘法减法括号（需更多 IR 改动）、AST rewrite（架构性改动，风险较高）。
