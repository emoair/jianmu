# v0.9 Dataset Archive Index

Historical datasets should remain reproducible from their existing paths. Future source packages should prefer manifests, reports, and audits rather than full dataset shards unless a human explicitly approves the size and scope.

Large JSONL shards and compiler traces should be referenced by manifest and checksum rather than copied into lightweight source review packages.

