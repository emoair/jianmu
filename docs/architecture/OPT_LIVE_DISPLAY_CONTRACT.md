# OPT Live Display Contract

OPT progress output is observational evidence only. It must be emitted periodically, flushed immediately, and mirrored to JSONL trace records. Correctness still comes from backend compiler manifests, subprocess return codes, artifact hashes, and stdout comparison.
