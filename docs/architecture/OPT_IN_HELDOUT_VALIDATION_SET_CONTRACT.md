# Opt-in Heldout Validation Set Contract

The heldout validation set is not a training set. It uses existing generators and existing bridge logic only.

No external data, external API, model training, or weight update is allowed.
