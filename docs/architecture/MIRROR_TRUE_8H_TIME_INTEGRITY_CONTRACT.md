# Mirror True 8h Time Integrity Contract

The 8-hour claim is valid only when actual monotonic elapsed seconds are at least 28,800 and every cycle records actual elapsed seconds of at least 3,600. Planned duration, configured cycle count, or sleep padding cannot be used as actual elapsed evidence.

Heartbeat span must be present and must not exceed actual elapsed time.

