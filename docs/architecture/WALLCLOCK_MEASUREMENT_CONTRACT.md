# Wall-clock Measurement Contract

Every wall-clock validation run records planned hours, actual hours, actual elapsed seconds, monotonic start/end, UTC start/end, and whether the wall-clock minimum was satisfied by actual monotonic elapsed time.

`planned_duration_used_as_actual` must remain false.
