# Dataset Artifact Repo Hygiene Contract

Large dataset artifacts must stay outside the Git worktree. The repository may store summaries, indexes, manifests, and representative evidence packs, but it must not store full generated dataset payloads or compiler artifacts in project root.
