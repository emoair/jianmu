# Controlled Opt-in Support Candidate Contract

v1.0.8 is a review layer over the v1.0.7 staged opt-in profile, v1.0.6 dry-run adapter, and v1.0.5.x AtomicSynthesis / ExtendedIR / ExtendedEmitterC bridge.

The contract requires explicit opt-in, traceable compiler validation for supported shapes, safe rejection for unsupported boundaries, rollback safety, and production support flags remaining false.
