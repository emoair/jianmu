# NL to SymbolBindingToken Contract

Chinese rename and refactor requests can map to SymbolBindingToken records only when symbol identity, scope, and string/comment guards remain explicit. The natural language layer cannot rewrite identifiers by itself.
