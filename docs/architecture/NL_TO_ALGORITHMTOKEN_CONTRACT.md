# NL to AlgorithmToken Contract

Chinese algorithm requirements can be normalized into AlgorithmToken records only when the intent is bounded and auditable. Recursive or ambiguous requests stay future-domain or review unless the substrate explicitly supports them.
