# Opt-in Longhaul Accounting Rule

Longhaul accounting must distinguish validation events, real compiler invocations, unique compile units, replay, rollback, blocking checks, and trace writes.

Summary-only validation, cached-as-new results, duplicate invocation ids, stubbed validation, and syntax-filter-only correctness evidence are forbidden.
