# Classic C Algorithm Corpus Contract

ForgeCorpus contains controlled, single-file C-subset algorithm implementations generated deterministically or accepted through license-audited manual drop-in. Generated sources are allowed for training. Manual sources require source URL, commit, file path, source hash, and permissive license metadata.

Unsupported or unknown-halting samples must not carry trainable target_ir or expected_output.

