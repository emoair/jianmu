# RedQueen Endurance Lifecycle Contract

Endurance validation must reuse the v1.0.8.3.1 lifecycle cleanup guard. Each cycle records lifecycle checkpoints and the final run requires a post-run idle sentinel with no lingering Python, Git, compiler, generated executable, worker thread, manifest handle, or git index lock.
