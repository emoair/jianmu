# NL to CSystemsToken Contract

Chinese C systems requirements can map to CSystemsToken records for bounded pointer, malloc, struct, file-IO, and multi-file alpha probes. Safety boundaries, sandboxing, and compiler validation remain downstream of token mapping.
