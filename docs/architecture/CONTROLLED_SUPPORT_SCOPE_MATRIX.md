# Controlled Support Scope Matrix

The support candidate subsets are bounded function, array, function-array, and structured recursion shapes already exercised by the staged opt-in bridge.

Each subset requires explicit opt-in, is unreachable from the default profile, requires trace, and keeps `production_completed = false`.
