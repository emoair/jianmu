# Algorithm Token Contract

Algorithm sources may be converted into Project StandardToken, MirrorToken, or TuringToken. Tokens must not contain raw C source, raw target_ir JSON, or expected output. Token output is an adapter artifact, not correctness evidence.

Full compiler validation remains the final engineering check.

