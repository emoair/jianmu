# Pointer Frontier Contract

Allowed pointer patterns are address-of scalar, dereference assignment, pointer parameters, array traversal, output parameters, and const pointer reads.

The probe does not support arbitrary pointer arithmetic, complex pointer graphs, function pointers, `void *`, or dynamic linked structures.
