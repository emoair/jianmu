# RedQueen Stability Drift Contract

Stability drift includes decreasing plan-follow behavior, unsupported boundary review dropping to zero, stable categories being marked weak, synthetic signals leaking into the real compile lane, production-like behavior appearing, or default profile contamination.

Any such drift blocks `redqueen_multiround_stability_positive`.
