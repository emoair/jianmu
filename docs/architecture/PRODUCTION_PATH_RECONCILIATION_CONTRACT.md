# Production Path Reconciliation Contract

v1.0.5 may add an experimental active bridge from ExtendedIR to C source and compiler validation. It must not change the default production profile or claim production support for functions, arrays, or recursion.

Allowed claim level: bounded production runtime baseline plus experimental active bridge evidence.

Forbidden claim level: production readiness, solved synthesis, arbitrary project parsing, formal Turing completeness, natural language completion, or real promotion.

