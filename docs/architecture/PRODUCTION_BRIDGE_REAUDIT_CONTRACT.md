# Production Bridge Reaudit Contract

The v1.0.5.1 reaudit must first verify that each experimental policy reaches ExtendedIR, ExtendedEmitterC, C source generation, compiler invocation, executable run, and stdout comparison. Template renderers and marker IR may be used only as semantic references, not as final emitters.

