# Multi-File Build Contract

Multi-file samples must list files, build order, matching header declarations, include guards, and real compile/link evidence.

Arbitrary makefiles, CMake projects, external libraries, and unrestricted project layouts are not supported.
