# Human Review Signoff Contract

`REVIEW_SIGNOFF.md` is a template until a reviewer fills it in. An unchecked template never means approved.

Only an explicit checked approved decision may set `controlled_opt_in_support_approved = true`, and production support flags must remain false.
