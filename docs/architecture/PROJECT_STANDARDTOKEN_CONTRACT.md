# Project StandardToken Contract

Project StandardToken is a structured project descriptor token. It may contain module categories, feature flags, call-graph summaries, and support labels. It must not contain raw C source, raw target_ir JSON, or expected output.

Compiler/watchdog validation remains the correctness anchor. Syntax filtering is only a frontend efficiency tool.

