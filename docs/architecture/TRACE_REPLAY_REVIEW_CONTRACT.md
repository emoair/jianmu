# Trace Replay Review Contract

Trace replay must load sampled trace rows, deterministically rebuild the same policy path, re-emit C, recompile, rerun, and compare expected and actual stdout. Drift must be counted, not hidden.

