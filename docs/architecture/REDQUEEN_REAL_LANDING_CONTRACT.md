# RedQueen Real Landing Contract

RedQueen real landing means RedQueen controls validation distribution inside the staged opt-in lane only.

It may control category allocation, difficulty, review allocation, shape diversity, frontier pressure, boundary rechecks, rollback checks, and replay checks. It may not control default profile, production support flags, release, real promotion, external APIs, model weights, unsupported dangerous compilation, or compiler correctness semantics.
