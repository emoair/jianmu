# OPT Trace Backend Binding Contract

OPT trace rows must include backend cl/link/exe counters from the backend compiler lane and must point to the backend manifest used for audit. Frontend event counts and syntax-filter counts must remain separate from backend compiler invocation counts.
