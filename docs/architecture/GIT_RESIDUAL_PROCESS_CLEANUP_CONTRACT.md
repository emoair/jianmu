# Git Residual Process Cleanup Contract

Runs must snapshot Git for Windows processes before and after validation, remove stale index locks, classify root causes, and prevent worktree artifact storms. IDE-owned Git scans may be recorded as notes only when they are explained and non-blocking.
