# Active Work Threshold Calibration Contract

Active-work thresholds must be derived from observed backend compiler throughput, not from fixed wishful targets. The v1.0.8.8.3 20K/45min threshold is rejected when measured MSVC cl/link/exe throughput cannot support it despite clean correctness and active-window evidence.
