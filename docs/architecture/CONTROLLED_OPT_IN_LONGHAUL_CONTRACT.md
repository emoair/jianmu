# Controlled Opt-in Longhaul Contract

The longhaul run is a validation gate. It stresses the existing staged opt-in path for at least eight hours without changing the default runtime profile or enabling real promotion.

All positive results remain evidence for review, not production support.
