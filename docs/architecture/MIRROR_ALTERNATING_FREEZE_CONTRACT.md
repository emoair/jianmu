# Mirror Alternating-Freeze Contract

The mirror path is explicit opt-in only. It has one active lane and one frozen lane, or both lanes frozen for review only. Simultaneous active lanes are invalid. A frozen lane can provide comparison and feedback but cannot be mutated.

This contract does not enable default production support or real promotion.

