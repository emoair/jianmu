# RedQueen Metrics Bus Contract

The metrics bus is read-only. It aggregates validation success, compiler correctness, wrong stdout, timeout, replay drift, rollback, unsupported rejection, coverage, repeated-shape risk, and distribution evidence.
