# Real Compiler Accounting Rule

Real compiler validation must record real compiler invocation count, link count, executable run count, unique compile units, duplicate invocation IDs, cached result reuse, stubbed validation, and summary-only validation.

Cached, stubbed, duplicated, or syntax-only results cannot be counted as new correctness evidence.

