# Incremental Curriculum Dataset Contract

Incremental dataset training means curriculum case distribution training, not neural model weight updates. Dataset records must include sample id, category, difficulty, generator, RedQueen weight, Mirror feedback source, split, sha256, provenance, compiled status, heldout flag, and leakage guard hash.
