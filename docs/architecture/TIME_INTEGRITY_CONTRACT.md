# Time Integrity Contract

Actual elapsed time must be measured from a monotonic clock. Planned duration, target duration, cycle count, event count, and configured minimum hours cannot be reported as actual elapsed time.

When a record lacks monotonic start/end, UTC start/end, cycle elapsed values, and heartbeat span evidence, any endurance claim based on that record remains unverified.
