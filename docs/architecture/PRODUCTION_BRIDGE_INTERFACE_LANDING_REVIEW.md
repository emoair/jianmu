# Production Bridge Interface Landing Review

Interface review checks that policies reach builders, builders produce ExtendedIR, ExtendedEmitterC emits C, the compiler backend invokes cl.exe, executables run, and stdout is compared. Direct template compilation and marker IR direct compile are forbidden.

