# Opt-in Rollback and Trace Contract

Each opt-in execution must write audit trace rows with policy path, compiler invocation, stdout comparison, and opt-in metadata.

Opt-out rollback must disable bridge reachability, preserve arithmetic baseline behavior, keep production flags false, and leave default profile unchanged.
