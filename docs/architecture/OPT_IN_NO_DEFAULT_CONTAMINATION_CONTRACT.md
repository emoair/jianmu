# Opt-in No Default Contamination Contract

Without the explicit opt-in flag, function, array, recursion, and mixed bridge requests must be blocked from the new bridge.

The arithmetic baseline and existing runtime actions remain the compatibility baseline. The default profile must not import, route to, or depend on the staged opt-in profile.
