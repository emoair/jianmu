# Windows Memory Attribution Contract

System memory attribution must distinguish runner memory, child process tree, external scanner/IDE processes, cache/standby, commit, pool, and unaccounted pressure.
