# Frontend / Backend Validation Lane Contract

Frontend lane covers sample generation, scheduling, mirror feedback, syntax prefiltering, shape diversity, OPT display, and metadata analysis.

Backend lane covers writing C source, invoking `cl.exe`, invoking `link.exe`, running the executable, capturing stdout, and comparing expected versus actual output.

