# RedQueen Endurance Cycle Contract

Each endurance cycle must load the current RedQueen plan, snapshot pre metrics, execute RedQueen-controlled validation distribution, collect post metrics, compute deltas, audit distribution effect, update the next plan, run lifecycle checkpoint, and preserve default profile and production flags.
