# V1.0.3 Algorithm Substrate Plan

v1.0.3 adds ForgeCorpus, a deterministic classic C algorithm substrate. It validates algorithm family coverage, algorithm-to-token conversion, syntax frontend filtering, full compile validation, heldout variants, RedQueen curriculum, Symbiote metrics, and comfort-zone risk.

It does not change the default profile, enable real promotion, or claim arbitrary project parsing.

