# v1.0.3.1 Algorithm Variant Scale Plan

This development pass adds the ForgeCorpus algorithm variant scale probe. It expands semantic skeletons into requirement-driven C variants, writes a large controlled dataset, audits sample accounting and freshness, validates token hygiene, runs syntax frontend checks, and uses full compile/run/stdout for correctness evidence.

Large scale must be attempted. Full scale may be skipped with an explicit reason. No real promotion or default runtime change is allowed.
